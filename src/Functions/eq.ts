/**
 * @param num1 The number to compare
 * @param num2 The other number to compare
 * @returns {boolean} Returns `true` if both numbers are equal, and `false` if they are not
 */
export default function eq(num1: number, num2: number): boolean {
    return num1 == num2;
}