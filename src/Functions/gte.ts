/**
 * @param num1 The number to compare
 * @param num2 The other number to compare
 * @returns {boolean} Returns `true` if `num1` is greater than or equal to `num2`, and `false` if it's not
 */
export default function gte(num1: number, num2: number): boolean {
    return num1 >= num2;
}